syntax = "proto3";

package auth;

service AuthService {
  rpc CheckUser (UserRequest) returns (UserResponse);
}

message UserRequest {
  string email = 1;
}

message UserResponse {
  bool exists = 1;
}